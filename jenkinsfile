pipeline {
    agent any

    environment {
        APP_USERNAME = 'mrzee@gmail.com'
        APP_PASSWORD = 'ziA@9566,'
    }

    stages {
        stage('Install Dependencies') {
            steps {
                bat 'pip install -r requirements.txt'
            }
        }

        stage('Run Tests') {
            steps {
                bat 'pytest -v'
            }
        }
    }
}